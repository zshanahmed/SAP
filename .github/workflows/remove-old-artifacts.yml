name: Remove old artifacts
on:
  push:
    branches: 
      - fix_ci_cd
#on:
#  schedule:
#    # Every day at 1am
#    - cron: '0 1 * * *'

jobs:
  remove-old-artifacts:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Remove old artifacts
      uses: UIOWAjohnsonhj/001_SEP2021@v1
      with:
        age: '1 month'
        # Optional inputs
        # skip-tags: true
        # skip-recent: 5
