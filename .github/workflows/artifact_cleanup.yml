name: 'nightly artifacts cleanup'
on:
  push:
    branches: 
      - fix_ci_cd

jobs:
  delete-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: kolpav/purge-artifacts-action@v1
        with:
          token: $
          expire-in: 2days # Set this to 0 to delete all artifacts
